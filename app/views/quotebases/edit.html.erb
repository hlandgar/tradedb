<%= provide(:title, "Edit Quotes") %>
<h1>Edit a quote</h1>

<div class="row">
	<div class="span6 offset3">
		<%= form_for @quote do |f| %>
			<%= render 'quote_fields', f: f %>

			<%= f.submit "Update", class: "btn btn-large btn-primary" %>
		<% end %>
	</div>
</div>